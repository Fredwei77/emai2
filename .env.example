# Copy this file to .env and fill your OpenRouter API key.
#
# Usage (dev):
# 1) cp .env.example .env
# 2) Edit .env and set your key
# 3) npm run dev
#
# Vite dev server will load this .env and populate process.env.OPENROUTER_API_KEY
# for the local API proxy at /api/ai-chat and /api/ai-chat/stream.
#
# IMPORTANT: Never commit your real key. .gitignore already excludes .env.

OPENROUTER_API_KEY=

# SMTP 邮件服务配置（用于联系表单发送邮件）
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
MAIL_TO=107251567@qq.com

# Whitelisting for dev server endpoints (vite only)
# Comma-separated list. You can use full origins (https://example.com) or hostnames (example.com)
ALLOWED_ORIGINS=
# Comma-separated IP addresses allowed to call dev endpoints (default allows only localhost)
ALLOWED_IPS=
# Non-stream upstream request timeout in milliseconds
DEV_TIMEOUT_MS=25000
# Idle timeout for streaming (SSE) in milliseconds
DEV_STREAM_IDLE_TIMEOUT_MS=120000

